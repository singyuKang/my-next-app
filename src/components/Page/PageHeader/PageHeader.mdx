import { Meta, Stories, Subtitle, Title } from '@storybook/blocks'
import PageHeaderStories from './PageHeader.stories'

<Meta of={PageHeaderStories} />

<Title>Page Header</Title>
<Subtitle>페이지에 기본으로 존재하는 헤더</Subtitle>

특수한 페이지를 제외하고 사용할 수 있다.\
Page Header는 h2 엘리먼트를 기본 값으로 한다.

<Stories />
